{
  "name": "ecomerce",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "lint": "yarn workspace frontend run lint && yarn workspace backend run lint",
    "dev:front": "yarn workspace frontend run dev",
    "dev:back": "yarn workspace backend run start:dev"
  }
}
